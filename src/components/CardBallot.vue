<script setup>
const props = defineProps(['title'])
</script>
<template>
    <div class="w-3/4">
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>
            <h3>{{ props.title }}</h3>

    </div>
</template>